const nav=document.getElementById('nav');addEventListener('scroll',()=>nav.classList.toggle('scrolled',scrollY>35));
const io=new IntersectionObserver(es=>es.forEach(e=>e.isIntersecting&&e.target.classList.add('visible')),{threshold:.12});document.querySelectorAll('[data-reveal]').forEach(e=>io.observe(e));
document.querySelectorAll('.filter').forEach(btn=>btn.onclick=()=>{document.querySelectorAll('.filter').forEach(b=>b.classList.remove('active'));btn.classList.add('active');const f=btn.dataset.filter;document.querySelectorAll('.work').forEach(w=>w.style.display=f==='all'||w.dataset.cat===f?'block':'none')});
document.querySelectorAll('.comparebox').forEach(box=>{const range=box.querySelector('input'),after=box.querySelector('.after'),line=box.querySelector('.compareline'),knob=box.querySelector('.compareknob');range.oninput=e=>{const v=e.target.value;after.style.clipPath=`inset(0 0 0 ${v}%)`;line.style.left=v+'%';knob.style.left=v+'%'}});
const modal=document.getElementById('modal'),mi=modal.querySelector('img');document.querySelectorAll('.work').forEach(w=>w.onclick=()=>{mi.src=w.querySelector('img').src;modal.classList.add('open')});modal.querySelector('button').onclick=()=>modal.classList.remove('open');modal.onclick=e=>{if(e.target===modal)modal.classList.remove('open')};addEventListener('keydown',e=>e.key==='Escape'&&modal.classList.remove('open'));